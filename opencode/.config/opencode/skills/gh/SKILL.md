# Using `gh` commands on the command line, bash, or zsh

## Making comments and descriptions

If you have a multi-line body or description, prefer to use the `--body-file` argument instead of `--body`. This will make sure that the white space like `\n` are not displayed on githubs website.
